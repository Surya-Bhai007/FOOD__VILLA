import React from "react";
import ReactDOM from "react-dom/client";


const header =(
    <h1>starting tools</h1>
);














//Initializing Root and render
const Root = ReactDOM.createRoot(document.getElementById("root"));
Root.render(header);
